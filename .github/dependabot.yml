# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2

updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    groups:
      actions:
        patterns:
          - "*"
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    groups:
      cargo:
        patterns:
          - "*"
  - package-ecosystem: uv
    directory: /
    schedule:
      interval: "weekly"
    cooldown:
      default-days: 7
    groups:
      uv-pip:
        patterns:
          - "*"
  ## Deno support in beta did not pan out. We'll try again when it goes stable.
  # - package-ecosystem: deno
  #   directories:
  #     - /
  #     - /common
  #     - /crates/fontawesome
  #     - /crates/mdi
  #     - /crates/octicons
  #     - /crates/simple-icons
  #   schedule:
  #     interval: "daily"
  #     # run sunday, tuesday, and thursday
  #     # cronjob: 0 0 * * 0,2,4
  #   groups:
  #     deno:
  #       patterns:
  #         - "*"
